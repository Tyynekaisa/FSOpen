{
  "name": "blogilista",
  "version": "0.0.1",
  "description": "Fullstack Open - Osa 4 - Blogilista",
  "license": "MIT",
  "author": "Kaisa Juhola",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "blogilista": "node --watch blogilista.js",
    "dev": "node --watch index.js",
    "test": "node --test",
    "build:ui": "rm -rf dist && cd ../blogilista_frontend && npm run build && cp -r dist ../blogilista_backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "lint": "eslint ."
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "mongoose": "^9.9.1",
    "morgan": "^1.11.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "eslint": "^10.8.0",
    "globals": "^17.9.0"
  }
}
